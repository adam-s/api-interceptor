# Exploration 07: Next.js

Added Next.js 16 with App Router, React 19, Tailwind v4 to `apps/web/`. Uses `transpilePackages` for raw TypeScript workspace dependencies. Includes Dockerfile with turbo prune and multi-stage build.

No blog post — infrastructure-only exploration. See PR #5.
