# Exploration 06: Vitest

Added Vitest v4 with workspace projects. Root `vitest.config.ts` uses `projects` array pointing to per-package `defineProject` configs. Each package has `passWithNoTests: true` until tests are added.

No blog post — infrastructure-only exploration. See PR #4.
