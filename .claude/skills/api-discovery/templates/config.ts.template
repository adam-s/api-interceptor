/**
 * {{DOMAIN_DISPLAY}} Interceptor Configuration
 *
 * Captures API traffic from {{ROOT_DOMAIN}} and its subdomains.
 */

import type { InterceptorConfig } from '@interceptor/browser/shared/config';
import { z } from 'zod';

export const {{DOMAIN_NAME}}InterceptorConfig: InterceptorConfig = {
	domainName: '{{DOMAIN_NAME}}',

	interceptPatterns: [
		// Add patterns discovered from traffic capture
		// Example: 'https://api.{{ROOT_DOMAIN}}/**'
	],

	requiredHeaders: [],

	headerSchema: z.object({
		Cookie: z.string().optional(),
	}),

	baseUrls: [
		// Add base URLs discovered from traffic capture
		// Example: 'https://www.{{ROOT_DOMAIN}}'
	],
};
