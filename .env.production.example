# ==============================================================================
# PRODUCTION ENVIRONMENT — BrowserAgent
# ==============================================================================
# Copy to .env and fill in real values before deploying.

# Auth (generate secret with: openssl rand -base64 32)
AUTH_SECRET=CHANGE_ME
AUTH_URL=https://your-domain.com

# Runtime
NODE_ENV=production
